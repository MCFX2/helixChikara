class NearMissReciever : ZilchComponent
{
  function NearMissSuccess(){
    this.GameSession.GlobalVariables.AddToScore(10);
    
    if(this.Space.MarkedForDestruction) return;
    this.Space.CreateAtPosition(Archetype.NearMiss, Real3(0, 7.5, 1));
  }
}
